---
description: "기능 개발용 Agent Team 생성. 병렬로 설계/구현/테스트를 동시 진행한다."
allowed-tools: Teammate, Task, TaskCreate, TaskUpdate, Read, Write, Bash, Grep, Glob
---

# 🚀 Feature Development Team

## 목표
$ARGUMENTS 기능을 병렬 개발 팀으로 구현한다.

## 사전 준비
1. 먼저 현재 프로젝트의 구조와 기술 스택을 파악한다 (package.json, requirements.txt, go.mod 등)
2. CLAUDE.md가 있으면 읽어서 프로젝트 컨벤션을 파악한다
3. 관련 코드를 탐색해서 영향 범위를 파악한다

## 팀 구성 (3~4명)

### Plan Mode로 먼저 설계
팀을 생성하기 전에, 구현 계획을 수립한다:
- 독립적으로 병렬 작업 가능한 단위로 분해
- 각 단위 간 의존성 파악
- Wave 구조로 정리 (의존성 없는 것들이 Wave 1)

### 팀 생성
```
Create an agent team for feature development: "$ARGUMENTS"

Spawn teammates with these roles:
1. **architect** (Sonnet): 설계 및 인터페이스 정의. 타입/스키마/API 계약을 먼저 만들고 다른 팀원에게 공유
2. **implementer** (Sonnet): architect가 정의한 인터페이스 기반으로 핵심 로직 구현
3. **tester** (Sonnet): 테스트 코드 작성. architect의 인터페이스가 나오면 바로 테스트 뼈대를 만들고, implementer 완료 후 통합 테스트 실행

Task dependencies:
- "인터페이스 정의" → 의존성 없음 (Wave 1)
- "핵심 로직 구현" → blocked by "인터페이스 정의"
- "테스트 뼈대 작성" → blocked by "인터페이스 정의"  
- "통합 테스트" → blocked by "핵심 로직 구현", "테스트 뼈대 작성"

Use Sonnet for all teammates to manage costs.
```

## 리더 역할
- 팀원 간 진행 상황을 모니터링
- 인터페이스 충돌이나 불일치가 발생하면 즉시 중재
- 모든 태스크 완료 후 통합 검증 수행
- git branch 생성 및 커밋 관리

## 완료 조건
- [ ] 모든 태스크가 completed 상태
- [ ] 테스트가 전부 통과
- [ ] 리더가 코드를 리뷰하고 통합
- [ ] 커밋 메시지 작성 (conventional commits)
